#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.join(__dir__, "..", "lib"))
require "dup_assets"

root = ARGV[0] || "app/assets"

unless Dir.exist?(root)
  warn "#{root}: directory not found"
  exit 1
end

exit DupAssets.call(root)
