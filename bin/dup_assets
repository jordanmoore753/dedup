#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.join(__dir__, "..", "lib"))
require "dup_assets"

root = ARGV[0]

if root && !Dir.exist?(root)
  warn "#{root}: directory not found"
  exit 1
end

exit root ? DupAssets.call(root) : DupAssets.call
